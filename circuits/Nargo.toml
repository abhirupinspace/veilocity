[package]
name = "veilocity_circuits"
type = "lib"
authors = ["Veilocity Team"]
compiler_version = ">=1.0.0-beta.16"

[dependencies]
# Poseidon hash library (BN254 compatible)
# Note: Noir's std library includes poseidon, so explicit dependency may not be needed
